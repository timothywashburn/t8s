{{- range .Values.applications }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .name }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ .repo_url }}
    path: {{ .path }}
    targetRevision: {{ .branch }}
    helm:
      {{- if .values_file }}
      valueFiles:
        - {{ .values_file }}
      {{- end }}
      {{- if .values }}
      values: |
{{ toYaml .values | indent 8 }}
      {{- end }}
      {{- if or .images $.Values.cloudflare_tunnel.enabled }}
      parameters:
        {{- if $.Values.cloudflare_tunnel.enabled }}
        - name: ingress.tls
          value: "false"
        {{- end }}
        {{- range .images }}
        - name: image.{{ .alias }}.name
          value: {{ .image }}
        {{- end }}
      {{- end }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .namespace }}
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - kind: PersistentVolumeClaim
      jsonPointers:
        - /spec/storageClassName
    - kind: Secret
      jsonPointers:
        - /data
        - /stringData
{{- end }}
